dependencies {
  api deps.gson

  testImplementation project(':methanol-testing')
}

rootProject.addReads(compileTestJava, 'methanol.convert.gson', 'methanol.testing')
rootProject.addReads(test, 'methanol.convert.gson', 'methanol.testing')
