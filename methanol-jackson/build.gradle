dependencies {
  api project(':methanol')
  api deps.jacksonDatabind

  testImplementation project(':methanol-testing')
}

rootProject.addReads([compileTestJava, test], 'methanol.testing')

test {
  moduleOptions {
    def pkg ='com.github.mizosoft.methanol.convert.jackson'
    addOpens = [("$moduleName/$pkg" as String): 'com.fasterxml.jackson.databind']
  }
}
